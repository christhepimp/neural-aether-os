# Kernel experiments

Empty on purpose.

When Stage 4 starts, put here:

- eBPF programs that feed `/proc` and sched events into the control plane
- a Linux module sketch for `/dev/aether`
- QEMU + Rust boot notes

Do not confuse a README with a kernel.
